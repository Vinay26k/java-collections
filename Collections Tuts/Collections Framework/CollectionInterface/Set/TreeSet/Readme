TreeSet implements NavigableSet Interface

##########################################
#                                        #
#            TreeSet Class               #
#        i.e., java.util.TreeSet         #
#                                        #
##########################################

TreeSet Class
    - we can add objects into a tree set as long as
    they can be compared with each other
    - access and retrieval times are quite fast

TreeSet Constructor
    - TreeSet()
    - TreeSet(Collection c)
    - TreeSet(Comparator c)
    - TreeSet(SortedSet s)

java.util.Comparator Interface methods [abstract]:
    - int compare(Object o1, Object o2)
    - boolean equals(Object o)

java.lang.Comparable Interface methods [abstract]:
    - int compareTo(Object o)